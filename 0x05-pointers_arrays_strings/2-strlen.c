#include "main.h"
#include <stdio.h>

/**
 * _strlen - check the code
 *
 * @s: Returns the length of a string
 *
 * Return: Always 0 (Success)
 */

int _strlen(char *s)
{
	int count = 0;
	int i;

	for (i = 0; i++);
	{
		if (s[i] == '\0')
			break;
		count++;
	}

	return (count);
}
